version: '3.7'

services:
  wallet-exporter:
    entrypoint: ["echo", "wallet_exporter disabled."]
    restart: "no"
  treasury-exporter:
    entrypoint: ["echo", "treasury_exporter disabled."]
    restart: "no"
  historical-treasury-exporter:
    entrypoint: ["echo", "historical_treasury_exporter disabled."]
    restart: "no"
  transactions-exporter:
    entrypoint: ["echo", "transactions_exporter disabled."]
    restart: "no"
  historical-ftm-exporter:
    entrypoint: ["echo", "historical-ftm-exporter disabled."]
    restart: "no"
  ftm-exporter:
    entrypoint: ["echo", "ftm-exporter disabled."]
    restart: "no"
    
  historical-eth-exporter:
    entrypoint: ["echo", "historical-eth-exporter"]
    restart: "no"

  eth-exporter:
    entrypoint: ["echo", "eth-exporter"]
    restart: "no"