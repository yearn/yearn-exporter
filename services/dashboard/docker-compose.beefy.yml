version: '3.7'

services:
  historical-exporter:
    environment:
      - POOL_SIZE=4
  wallet-exporter:
    environment:
      - POOL_SIZE=4
  treasury-exporter:
    environment:
      - POOL_SIZE=4
  historical-treasury-exporter:
    environment:
      - POOL_SIZE=4
  transactions-exporter:
    environment:
      - POOL_SIZE=4
